#include <cassert>
#include <libtcod.hpp>
#include "Monster.hpp"

/*int Monster::GetPositionX()
{
	return m_positionX;
}

int Monster::GetPositionY()
{
	return m_positionY;
}

void Monster::SetPosition(int x, int y)
{
	assert(x >= 0 && y >= 0);
	m_positionX = x;
	m_positionY = y;
}

void Monster::SetGlyph(char glyph)
{
	m_glyph = glyph;
}

void Monster::SetColor(TCODColor color)
{
	m_color = color;
}

void Monster::Draw()
{
	TCODConsole::root->putCharEx(m_positionX, m_positionY, m_glyph, m_color, TCODColor::black);
}*/